<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @first_micropost.content %></td>
      <td><%= @first_micropost.user_id %></td>
      <td><%= link_to 'Show', @first_micropost %></td>
      <td><%= link_to 'Edit', edit_micropost_path(@first_micropost) %></td>
      <td><%= link_to 'Destroy', @first_micropost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  </tbody>
</table>
